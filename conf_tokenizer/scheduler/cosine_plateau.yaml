# Cosine annealing scheduler with plateau after T_max iterations
scheduler:
  _target_: schedulers.CosineAnnealingWithPlateau
  T_max: ${max_steps}
  eta_min: 1e-6
  warmup_steps: 10000  # Set to > 0 for linear warmup